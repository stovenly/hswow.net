/**
 * The countryside zones' ids, and the one measurement the demo hall needs to
 * hang its door off the village's arch.
 *
 * The places themselves are documents under `content/zones/`; this is what the
 * zones that are still code have to name.
 */

export const ZONE_COUNTRYSIDE = 'countryside-village';
export const ZONE_COTTAGE = 'countryside-cottage';
export const ZONE_WORKSHOP = 'countryside-workshop';
export const ZONE_STORE = 'countryside-store';

/** Where the arch stands, and the portal with it. */
export const COUNTRYSIDE_GATE: readonly [number, number] = [0, 22];
