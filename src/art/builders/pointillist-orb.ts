import { finishOrb } from '../fixture';
import { PALETTE } from '../palette';

/** The marble berry: film thickness differing cell to cell, so it reads pixelated. */
export const pointillistOrb = finishOrb('pointillist-orb', PALETTE.INK, 'pointillist');
