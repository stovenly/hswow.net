import { finishOrb } from '../fixture';
import { PALETTE } from '../palette';

/** A black mirror with a starfield in it. The constellations swing as you walk round. */
export const voidstoneOrb = finishOrb('voidstone-orb', PALETTE.STONE, 'voidstone');
