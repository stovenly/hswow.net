import { finishOrb } from '../fixture';
import { PALETTE } from '../palette';

/** Anisotropy on metal: one lathe axis, a stretch that turns with the facets. */
export const brushedOrb = finishOrb('brushed-orb', PALETTE.IRON_PALE, 'brushed');
