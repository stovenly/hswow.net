import { finishColumn } from '../fixture';
import { PALETTE } from '../palette';

/** Domains flooding across a shaft, where a flood has room to be a region. */
export const schillerColumn = finishColumn('schiller-column', PALETTE.IRON_DARK, 'schiller');
