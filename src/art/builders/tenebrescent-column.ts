import { finishColumn } from '../fixture';
import { PALETTE } from '../palette';

/** Dark on the sunward faces, pale on the shaded ones: every value inverted. */
export const tenebrescentColumn = finishColumn('tenebrescent-column', PALETTE.STONE_PALE, 'tenebrescent');
