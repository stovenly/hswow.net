import { finishOrb } from '../fixture';
import { PALETTE, shade } from '../palette';

/** Liquid metal: the reflection crawls while the silhouette stands still. */
export const quickmetalOrb = finishOrb('quickmetal-orb', shade(PALETTE.CHROME, 0.74), 'quickmetal');
