import { finishOrb } from '../fixture';
import { PALETTE } from '../palette';

/** Glint on its own: micro-facets firing one at a time as the light turns. */
export const frostOrb = finishOrb('frost-orb', PALETTE.STONE_PALE, 'frost');
