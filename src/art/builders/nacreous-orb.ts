import { finishOrb } from '../fixture';
import { PALETTE, shade } from '../palette';

/** Mother-of-pearl: the film banded along the body's own axis rather than mottled. */
export const nacreousOrb = finishOrb('nacreous-orb', shade(PALETTE.WOOL, 1.22), 'nacreous');
