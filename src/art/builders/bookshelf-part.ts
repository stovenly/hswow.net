import { shelfCase } from '../shelf';

/**
 * The same case, part emptied.
 *
 * Some shelves untouched, some nearly bare, and the gaps where a book was taken
 * out — because a person empties a shelf at a time, and an even three-quarters
 * everywhere reads as a setting rather than as something that happened.
 *
 * The one to reach for when a room is meant to say somebody has been *using*
 * it. A full case is a library; this is a study in the middle of being packed.
 */
export const bookshelfPart = shelfCase('bookshelf-part', 'part');
