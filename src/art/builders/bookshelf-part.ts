import { shelfCase } from '../shelf';

// The same case, part emptied: some shelves untouched, some nearly bare, because
// a person empties a shelf at a time.
export const bookshelfPart = shelfCase('bookshelf-part', 'part');
