import { finishOrb } from '../fixture';
import { PALETTE } from '../palette';

/** Hackmanite: violet where the sun strikes, pale in its own shade. Everything inverted. */
export const tenebrescentOrb = finishOrb('tenebrescent-orb', PALETTE.STONE_PALE, 'tenebrescent');
