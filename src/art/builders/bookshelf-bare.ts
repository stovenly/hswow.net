import { shelfCase } from '../shelf';

// The carcass on its own, for a placer bringing their own books. The same case as
// `bookshelf` at the same seed, down to the joinery.
export const bookshelfBare = shelfCase('bookshelf-bare', 'empty');
