import { finishColumn } from '../fixture';
import { PALETTE } from '../palette';

/** A column-shaped hole with a sky in it, and the stars swing as you pass. */
export const voidstoneColumn = finishColumn('voidstone-column', PALETTE.STONE, 'voidstone');
