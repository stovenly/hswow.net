import { finishOrb } from '../fixture';
import { PALETTE } from '../palette';

/** The mirror end of the range: sky and sun disc in every facet. */
export const chromeOrb = finishOrb('chrome-orb', PALETTE.CHROME, 'chrome');
