import { finishColumn } from '../fixture';
import { PALETTE } from '../palette';

/** A mosaic over a whole shaft, coarsening honestly with distance. */
export const pointillistColumn = finishColumn('pointillist-column', PALETTE.INK, 'pointillist');
