import { finishDrape } from '../fixture';
import { PALETTE } from '../palette';

/** Sheen alone: rough, unstretched, and lit at the edges where it turns away. */
export const velvetDrape = finishDrape('velvet-drape', PALETTE.CLOTH_DEEP, 'velvet');
