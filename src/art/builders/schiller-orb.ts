import { finishOrb } from '../fixture';
import { PALETTE } from '../palette';

/** Labradorite: whole domains flood one at a time, hard-edged, as the stone turns. */
export const schillerOrb = finishOrb('schiller-orb', PALETTE.IRON_DARK, 'schiller');
