import { finishColumn } from '../fixture';
import { PALETTE, shade } from '../palette';

/** Bands run the height of the shaft, which is where growth lines would run. */
export const nacreousColumn = finishColumn('nacreous-column', shade(PALETTE.WOOL, 1.22), 'nacreous');
